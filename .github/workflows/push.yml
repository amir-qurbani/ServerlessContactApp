name: Push Code Check

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Verify files
        run: |
          echo "Listing repo files..."
          ls -R
